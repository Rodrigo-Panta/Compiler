program teste1
    
    a, b is int;
    result is int;
    a,x is float;

begin

    a = 12;
    x = 12;
    read (a);
    read (b);
    result = (a*b + 1) / (c+2);
    write ({Resultado: });
    write (result);
    read(asd)
    
end.